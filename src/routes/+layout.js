// Fully static site: prerender every route to HTML at build time.
export const prerender = true;

// Consistent trailing slashes so GitHub Pages serves directory-style URLs
// (e.g. /about/) without redirect surprises on the project subpath.
export const trailingSlash = 'always';
